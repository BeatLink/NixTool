# NixTool
A CLI tool for managing flake based NixOS installations. Built using Textual and Python

The Maintenance menu includes an entry to launch [nix-inspect](https://github.com/bluskript/nix-inspect), a TUI for browsing NixOS configurations and Nix expressions, run via `nix run github:bluskript/nix-inspect` against the configured flake path.
